# スレッドビュー for Chatwork

Chatwork の返信をスレッド形式で構造化表示する Chrome 拡張です。

**本拡張は Chatwork株式会社 の公式製品ではありません。** Chatwork は Chatwork株式会社 の商標です。
名称に含まれる「for Chatwork」は、対応サービスを示すための表記です。

## できること

- タイムライン上の返信関係を解析し、スレッドツリーとして右側パネルに表示
- スレッドを展開してツリーを閲覧
- ツリーのメッセージをクリックしてタイムラインの該当位置へジャンプ
- 「全件表示」で返信ゼロのメッセージも含めた一覧に切替
- ルーム切替と過去ログの追加読み込みに追従

## ソースコード

https://github.com/shngmsw/chatwork-thread-view

プライバシーポリシーで「通信しないことはソースコードで確認できる」と
書いているため、この URL は次の 4 箇所で一致させてください。

- `docs/lp/index.html` の `REPO_URL`
- `docs/store/listing.md` の基本情報
- この README
- `docs/store/privacy-policy.md` の「6. 検証方法」

## 設計方針

- バックエンドを持たず、ネットワーク通信を一切行いません
- API トークンなどの認証情報を一切扱いません
- 読み取るのは表示中のページの DOM だけです
- UI は Shadow DOM に閉じており、Chatwork 本体の CSS / JS と干渉しません

## インストール (開発版)

1. Chrome で `chrome://extensions` を開く
2. 「デベロッパー モード」を ON にする
3. 「パッケージ化されていない拡張機能を読み込む」でこのフォルダを選ぶ
4. Chatwork を開く / リロードする

### ソースを変更したあとの再読み込み (2 段階必要)

**拡張のリロードだけでは、既に開いているタブに新しいコードは入りません。**
content script は読み込み済みのタブへ再注入されないため、次の順に両方を行ってください。

1. `chrome://extensions` で本拡張の再読み込みボタンを押す
2. **そのうえで Chatwork のタブ自体をリロードする**

1 だけで済ませると、古いコードが動いたまま「直したはずの不具合が消えない」状態になります。

## 制約

- **DOM に読み込まれているメッセージのみが対象です。** Chatwork は履歴を遅延読み込みするため、
  上にスクロールして読み込んだ範囲でスレッドが復元されます。
  読み込み済み範囲外に親がある返信は「親メッセージ未読み込み」バッジ付きで単独表示されます。
- 別ルームへの返信は、そのルームを開いたときのみ親子として解決されます。
- Chatwork の画面構造が変わると動かなくなります。その場合はパネルに
  「Chatwork の画面構造が変わった可能性があります」と診断情報が表示されます。
  この診断は不調が 15 秒続いたときにだけ出ます (起動直後の読み込み待ちやルーム切替の一瞬を
  故障と誤認しないため)。投稿が無いルームでは出ません。
- 配色は Chatwork 本体のテーマ (ライト / ダーク) に追随します。

## 開発

```bash
npm install
npm test        # 一度だけ実行
npm run test:watch
```

Chatwork DOM への依存は `src/content/selectors.js` にのみ存在します。
画面構造が変わった場合はこのファイルを修正してください。
`runHealthCheck()` も同じファイルにあり、セレクタが噛み合っているかを検査します。
